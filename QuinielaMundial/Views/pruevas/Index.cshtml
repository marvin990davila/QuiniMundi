@model IEnumerable<QuinielaMundial.Models.pruevas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.prue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prudate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.prue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prudate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPrueva }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPrueva }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPrueva })
        </td>
    </tr>
}

</table>
